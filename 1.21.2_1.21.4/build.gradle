plugins {
	id 'net.fabricmc.fabric-loom-remap' version "${loom_version}"
	id 'maven-publish'
}

apply from: rootProject.file('shared.gradle')

sourceSets {
	main {
		java {
			srcDirs = [
				file('src/main/java'),                        // 1.21-1.21.1 specific (no sounds, String category, field access)
				rootProject.file('common/src/main/java')      // shared (InventoryFullOverlay)
			]
		}
		resources {
			srcDirs = [
				file('src/main/resources'),                   // 1.21-1.21.1 fabric.mod.json
				rootProject.file('common/src/main/resources') // shared (lang, icon)
			]
		}
	}
}